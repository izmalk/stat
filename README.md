# Spread Tutorial Automated Testing (STAT)

This is a solution for testing your tutorial using [Spread](https://github.com/canonical/spread). It parses tutorial sources, extracts all the commands from `shell` blocks, respects additional input from special comments, and runs all the commands in correct order via Spread in a virtual machine.
